#include <stdio.h>
/**
 * 배열 초기화
 * 배열 초기화는 선언 시 최초 한번만 가능
 * 그 이후로는 배열요소에 일일이 값을 대입
 * {} 사용해서 한번에 대입 불가능
 * 
 * 1. 기본적인 초기화
 * 
 * 2. 초기값이 배열개수보다 작은 경우
 *    기본값으로 채움:정수(0),실수(0,0)
 * 
 * 3. 모든 요소를 0으로 처리
 * int arr[1000]={0};
 * 
 * 4. 배열개수 생략된 경우
 *  초기값 개수만큼 배열 크기를 설정
 */
int main(){
    
}