#include<stdio.h>
/**
 * if문 기본조건
 * 실행문
 * else if(조건)
 * 실행문
 * else 
 * 실행문
 * 
 * if()로 시작
 * if와 else는 한번만 사용가능 else if는 여러번 반복 사용가능
 * if else if의 조건이 참이면 해당 블록문이 실행
 * if(조건) 시작해서 아래로 실행
 *  조건이 거짓이면 다음 else if문 실행
 *  조건이 참이면 블록문 실행 후 빠져 나감
 * else 모든 조건이 거짓일때 실행
 * 
 * if문 조합
 * 1. if()
 * 2. if() else
 * 3. if() else if()
 * 4. if() else if() else
 */
int main(){

}